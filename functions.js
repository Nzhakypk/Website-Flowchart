const createRecipeBtn = document.getElementById('create-recipe-btn');

createRecipeBtn.addEventListener('click', () => {
  create_new_recipe();
});

function create_new_recipe(): void {
  // тут можно написать логику для создания нового рецепта
}

